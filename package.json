{
  "name": "heroic",
  "bin": "build/index.js",
  "devDependencies": {
    "babel-plugin-root-import": "^5.1.0",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.6.1"
  },
  "scripts": {
    "pretest": "clear",
    "test": "cd build && npm i && node index",
    "debug": "cd source && nodemon -L --exec babel-node index.js",
    "prebuild": "rm -rf build && mkdir build && clear",
    "build": "cd source && babel . --out-dir=../build --copy-files --ignore node_modules",
    "compile": "pkg . --out-path compiled"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "browsers": [
              "last 2 Chrome versions"
            ]
          }
        }
      ]
    ],
    "plugins": [
      "transform-class-properties",
      "babel-plugin-root-import"
    ],
    "ignore": [
      "source/public/**/*"
    ]
  },
  "pkg": {
    "scripts": "build/app/**/*.js",
    "assets": [
      "build/public/**/*"
    ],
    "targets": [
      "node9-linux-x64",
      "node9-macos-x64",
      "node9-win-x64"
    ]
  }
}
